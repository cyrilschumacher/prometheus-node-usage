/**
 * Options.
 * @interface
 */
interface Options {
    /**
     * Formatters.
     * @type {OptionsFormatter}
     */
    formatters?: OptionsFormatters;

    /**
     * Prefix for metric name.
     * @type {string}
     */
    prefix?: string;
}
